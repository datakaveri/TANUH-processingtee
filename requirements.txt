Flask==2.3.2
gunicorn==21.2.0
werkzeug==2.3.7
psutil==5.9.2
cryptography>=41.0.0
python-dotenv>=1.0.0
PyYAML>=6.0
requests>=2.31.0
Flask-CORS>=4.0.0
# numpy: >=1.26.4 satisfies langchain; <2 satisfies onnxruntime-gpu 1.19.x ABI.
numpy>=1.26.4,<2
scikit-learn>=1.3.0
# CUDA 12.1 wheels (installed via the cu121 extra-index in the Dockerfile);
# compatible with the CUDA 12.3 runtime base image and driver 535.
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2
onnx>=1.14.0
# GPU build of onnxruntime — links libcudart.so.12 / libcudnn.so.9, matching
# the CUDA 12.3 + cuDNN 9 base. Replaces the CPU-only onnxruntime that silently
# fell back to CPUExecutionProvider.
onnxruntime-gpu==1.19.2
onnxscript>=0.1.0
pydicom>=2.4.0
pylibjpeg[all]>=2.0.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
transformers>=4.35.0
tokenizers>=0.15.0
accelerate>=0.26.0
datasets>=2.16.0
huggingface-hub>=0.20.0
safetensors>=0.4.0
sentencepiece>=0.1.99
diffusers>=0.25.0
peft>=0.8.0
trl>=0.7.0
bitsandbytes>=0.42.0
sentence-transformers>=2.3.0

# TensorFlow (CUDA 12 compatible)
tensorflow>=2.15.0

# JAX — installed separately in Dockerfile via the JAX CUDA wheel index
# (jax[cuda12] pip resolution is broken; see Dockerfile RUN block)

# Data / compute
scipy>=1.11.0
pandas>=2.0.0
polars>=0.20.0
pyarrow>=14.0.0
einops>=0.7.0

# Vector stores / retrieval
faiss-gpu-cu12>=1.7.4
chromadb>=0.4.0
qdrant-client>=1.7.0
pinecone>=3.0.0

# LLM clients / orchestration
openai>=1.10.0
anthropic>=0.18.0
langchain>=0.1.0
langgraph>=0.0.30
llama-index>=0.10.0

# Observability
wandb>=0.16.0
tensorboard>=2.15.0

# Utils
httpx>=0.26.0
