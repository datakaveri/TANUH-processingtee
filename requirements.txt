blinker==1.6.2
click==8.1.6
Flask==3.1.3
gunicorn==21.2.0
importlib-metadata==6.8.0
psutil==5.9.2
itsdangerous==2.1.2
Jinja2==3.1.2
MarkupSafe==2.1.3
packaging==23.1
werkzeug==2.3.7
zipp==3.16.2
cryptography>=41.0.0
python-dotenv>=1.0.0
PyYAML>=6.0
requests>=2.31.0
Flask-CORS>=4.0.0
# numpy pinned <2 — onnxruntime-gpu 1.19.x and torch 2.2.x cu121 wheels are
# built against the NumPy 1.x ABI.
numpy>=1.23.2,<2
scikit-learn>=1.3.0
# CUDA 12.1 wheels (installed via the cu121 extra-index in the Dockerfile);
# compatible with the CUDA 12.3 runtime base image and driver 535.
torch==2.2.2
torchvision==0.17.2
onnx>=1.14.0
# onnxruntime is installed in the Dockerfile via the ONNXRUNTIME_PKG build ARG
# (single source, one codebase, two images):
#   GPU build (default): onnxruntime-gpu==1.19.2 — links libcudart.so.12 /
#     libcudnn.so.9, matching the CUDA 12.3 + cuDNN 9 base (H100 path).
#   CPU build:           onnxruntime==1.19.2 — no CUDA provider .so, so it
#     cannot segfault on a GPU-less VM (CPU fallback path).
onnxscript>=0.1.0
pydicom>=2.4.0
pylibjpeg[all]>=2.0.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
transformers>=4.35.0
